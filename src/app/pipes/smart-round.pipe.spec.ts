import { SmartRoundPipe } from './smart-round.pipe';

describe('SmartRoundPipe', () => {
  it('create an instance', () => {
    const pipe = new SmartRoundPipe();
    expect(pipe).toBeTruthy();
  });
});
